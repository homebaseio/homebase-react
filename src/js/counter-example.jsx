import React from 'react';
// import { useTransact, useEntity } from '../localmost/react';

export const Counter = () => {

  return (
    <div>
      localmost React JS count: {"TODO count"}
      <div>
        <button onClick={() => {}}>
          Increment
        </button>
      </div>
    </div>
  )
}